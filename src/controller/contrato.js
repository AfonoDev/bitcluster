const {Op} = require("sequelize")
const Banco = require('../models/Banco')
const Usuario = require('../models/Usuario')
const Contrato = require('../models/Contrato')
const { store } = require("./usuario")

module.exports = {
    async store (req, res){
        
    }
}